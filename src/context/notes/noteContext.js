// This is simply creating the context. This must be included if we are willing to use Context API

import { createContext } from "react";

const noteContext = createContext();

export default noteContext;